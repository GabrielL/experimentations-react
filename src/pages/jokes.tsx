import { Card, CardContent, CardHeader } from "@mui/material";

export const Jokes = () => {
  return (
    <Card>
      <CardHeader title="jokes" />
      <CardContent>here are the jokes</CardContent>
    </Card>
  );
};
